batch_size: 8
num_workers: 2
max_epochs: 30
lr: 0.0001
weight_decay: 0.0005
warmup_epochs: 3
gradient_clip_val: 1.0

# Early stopping
patience: 5
min_delta: 0.001

# Checkpointing
save_top_k: 3
monitor: "val_map"
mode: "max"

# Resume training from a checkpoint (path to a .ckpt file, or null to start fresh)
resume_from: null

# Hardware
accelerator: "auto"
devices: 1
precision: "32"

# Seed for reproducibility
seed: 42
